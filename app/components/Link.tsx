"use client";

export const Link = ({}) => {
    return (
        <div>
            <button onClick={ () => {
                alert("ok")
            }}>
            LOGINS
            </button>
        </div>

    )
}